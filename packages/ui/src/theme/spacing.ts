import type { SpacingScale } from './types'

/**
 * design-system.md §1.4 — Espaciado. Unidad base 4px; MUI multiplica este valor
 * por el factor pasado a `theme.spacing(n)`, de forma consistente en ambos modos.
 */
export const spacingUnit: SpacingScale = 4
